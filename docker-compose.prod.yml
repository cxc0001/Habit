version: "3.8"

services:
  backend:
    build:
      context: .
      dockerfile: dockerfile
      target: runner
    image: habit-backend:latest
    container_name: habit-backend
    ports:
      - "12002:12002"
    environment:
      - NODE_ENV=production
      - PORT=12002
    volumes:
      - ./data:/app/server/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:12002/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    image: habit-frontend:latest
    container_name: habit-frontend
    ports:
      - "12001:12001"
    depends_on:
      - backend
    restart: unless-stopped