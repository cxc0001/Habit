# Dockerfile.frontend - 保持原端口配置
FROM nginx:alpine

# 复制完整的nginx主配置文件（覆盖默认）
COPY nginx.conf /etc/nginx/nginx.conf

# 复制构建好的前端文件
COPY dist /usr/share/nginx/html

# 暴露端口（保持原有）
EXPOSE 12001

CMD ["nginx", "-g", "daemon off;"]
